// type Coord = {
//     lat: number
//     long: number
// }

// type P = keyof Coord

// let a: P = 'long'

function log(a: string | null): void {
    if(a == null ){

    }else {
        a.toLocaleLowerCase
    }
    
}